#include <definitions.hpp>
#include <adapter.hpp>

// debug tools
/*
#ifdef _WIN32
#include <windows.h>
#define sleep(x) Sleep(1000 * x)
#else
#include <unistd.h>
#endif
*/

int main() {
    
    return 0;
}